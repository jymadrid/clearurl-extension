# 高级功能说明

## 功能概述

ClearURL 扩展现在包含两个强大的高级功能，将URL净化能力提升到新的维度：

1. **剪贴板自动净化**：自动净化复制的链接，确保分享的链接干净无跟踪
2. **短链接预解析**：穿透短链接伪装，展开并净化最终目标URL

## 功能一：剪贴板自动净化

### 功能描述

当用户从任何地方（聊天软件、PDF文档、网页等）复制包含跟踪参数的链接时，扩展会自动将其净化，确保粘贴时是干净的链接。

### 用户故事

> 作为一名注重隐私的用户，我经常需要和朋友分享链接。我希望在复制"脏链接"后，无需任何额外操作，粘贴时它就已经变成了干净版本，这样我就能放心地在任何地方分享它。

### 技术实现

#### 1. 权限要求

```json
{
  "permissions": [
    "clipboardRead",
    "clipboardWrite",
    "alarms",
    "notifications"
  ]
}
```

#### 2. 核心机制

**定期检查**：使用 `chrome.alarms` API 每2秒检查一次剪贴板

```javascript
chrome.alarms.create('clipboardCheck', { periodInMinutes: 0.033 }); // 约2秒

chrome.alarms.onAlarm.addListener((alarm) => {
  if (alarm.name === 'clipboardCheck' && this.clipboardCleaningEnabled) {
    this.monitorClipboard();
  }
});
```

**监控流程**：

```javascript
async monitorClipboard() {
  // 1. 读取剪贴板内容
  const text = await navigator.clipboard.readText();

  // 2. 避免重复处理
  if (text === this.lastClipboardContent) return;

  // 3. 验证是否为有效URL
  if (!this.isValidUrl(text)) return;

  // 4. 净化URL
  const cleanedUrl = this.cleanUrl(text);

  // 5. 只有在URL发生变化时才写入
  if (text !== cleanedUrl) {
    await navigator.clipboard.writeText(cleanedUrl);
    this.lastClipboardContent = cleanedUrl;

    // 6. 发送通知
    chrome.notifications.create({
      type: 'basic',
      iconUrl: 'icons/icon48.png',
      title: 'ClearURL',
      message: '链接已自动净化',
      priority: 0,
    });
  }
}
```

#### 3. URL验证

```javascript
isValidUrl(string) {
  try {
    const url = new URL(string);
    return url.protocol === 'http:' || url.protocol === 'https:';
  } catch {
    return false;
  }
}
```

### 使用方法

1. **启用功能**
   - 打开扩展弹出窗口
   - 在"高级功能"部分找到"自动净化剪贴板"
   - 打开开关

2. **使用体验**
   - 从任何地方复制包含跟踪参数的链接
   - 扩展会自动检测并净化
   - 收到通知："链接已自动净化"
   - 粘贴时得到干净的链接

### 性能优化

1. **避免重复处理**：记录上次处理的内容，避免重复净化
2. **静默错误处理**：忽略"Document is not focused"等常见错误
3. **按需启用**：默认关闭，用户主动开启

### 隐私保护

- 所有处理都在本地完成
- 不上传任何剪贴板内容
- 不记录剪贴板历史
- 用户完全控制功能开关

## 功能二：短链接预解析

### 功能描述

在浏览器实际跳转前，预先解析短链接（如 t.co, bit.ly）的最终目标，并对目标URL进行净化，根除隐藏在重定向过程中的跟踪行为。

### 用户故事

> 当我在社交媒体上看到一个 t.co 的链接时，我不知道它最终会指向哪里，也不知道其中隐藏了多少跟踪器。我希望 ClearURL 能在我点击它时，像"X光"一样看透它，直接带我到一个干净的目的地。

### 技术实现

#### 1. 短链接域名列表

维护一个可更新的短链接服务商域名列表 `shortener_domains.json`：

```json
[
  "t.co",
  "bit.ly",
  "goo.gl",
  "ow.ly",
  "is.gd",
  "tinyurl.com",
  ...
]
```

#### 2. 核心机制

**导航拦截**：

```javascript
chrome.webNavigation.onBeforeNavigate.addListener((details) => {
  if (details.frameId === 0) {
    this.handleShortUrlNavigation(details);
  }
});
```

**短链接检测**：

```javascript
isShortUrl(url) {
  try {
    const urlObj = new URL(url);
    const hostname = urlObj.hostname.toLowerCase();

    return this.shortenerDomains.some(domain =>
      hostname === domain || hostname.endsWith('.' + domain)
    );
  } catch {
    return false;
  }
}
```

**URL解析**：

```javascript
async resolveShortUrl(url, maxRedirects = 5) {
  let currentUrl = url;
  let redirectCount = 0;

  while (redirectCount < maxRedirects) {
    try {
      // 使用 HEAD 请求，不下载页面内容
      const response = await fetch(currentUrl, {
        method: 'HEAD',
        redirect: 'manual',
      });

      // 检查是否有重定向
      if (response.status >= 300 && response.status < 400) {
        const location = response.headers.get('Location');
        if (!location) break;

        // 处理相对URL
        currentUrl = new URL(location, currentUrl).href;
        redirectCount++;
      } else {
        break;
      }
    } catch (error) {
      console.error('Error resolving short URL:', error);
      break;
    }
  }

  return currentUrl;
}
```

**处理流程**：

```javascript
async handleShortUrlNavigation(details) {
  if (!this.shortUrlExpansionEnabled) return;
  if (!this.isShortUrl(details.url)) return;

  try {
    // 1. 显示加载状态
    chrome.action.setBadgeText({ text: '...' });
    chrome.action.setBadgeBackgroundColor({ color: '#FFA500' });

    // 2. 解析短链接
    const resolvedUrl = await this.resolveShortUrl(details.url);

    // 3. 净化最终URL
    const cleanedUrl = this.cleanUrl(resolvedUrl);

    // 4. 更新标签页
    if (cleanedUrl !== details.url) {
      await chrome.tabs.update(details.tabId, { url: cleanedUrl });

      // 5. 发送通知
      chrome.notifications.create({
        type: 'basic',
        iconUrl: 'icons/icon48.png',
        title: 'ClearURL',
        message: '短链接已展开并净化',
        priority: 0,
      });
    }

    // 6. 恢复徽章
    this.updateBadge();
  } catch (error) {
    console.error('Error handling short URL:', error);
    this.updateBadge();
  }
}
```

### 使用方法

1. **启用功能**
   - 打开扩展弹出窗口
   - 在"高级功能"部分找到"短链接预解析"
   - 打开开关

2. **使用体验**
   - 点击短链接（如 t.co, bit.ly）
   - 扩展图标显示"..."加载状态
   - 自动跳转到净化后的最终URL
   - 收到通知："短链接已展开并净化"

### 性能优化

1. **HEAD请求**：只获取响应头，不下载页面内容
2. **重定向限制**：最多跟随5次重定向，防止无限循环
3. **手动重定向**：使用 `redirect: 'manual'` 控制重定向过程
4. **异步处理**：不阻塞浏览器主线程

### 安全考虑

1. **最大重定向次数**：防止恶意重定向循环
2. **错误处理**：捕获并记录所有错误
3. **超时保护**：fetch 请求有默认超时
4. **用户控制**：默认关闭，用户主动开启

## UI/UX 设计

### 功能开关

```
┌─────────────────────────────────────┐
│ 高级功能                             │
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ 自动净化剪贴板          [开关]  │ │
│ │ 复制链接时自动净化跟踪参数      │ │
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ 短链接预解析            [开关]  │ │
│ │ 自动展开并净化短链接            │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 视觉反馈

1. **剪贴板净化**
   - 桌面通知："链接已自动净化"
   - 优先级：低（不打扰用户）

2. **短链接解析**
   - 徽章显示："..."（加载中）
   - 徽章颜色：橙色 (#FFA500)
   - 完成后恢复正常徽章
   - 桌面通知："短链接已展开并净化"

## 数据存储

### 存储结构

```javascript
{
  clipboardCleaningEnabled: false,
  shortUrlExpansionEnabled: false
}
```

### 存储位置

- `chrome.storage.local`
- 与其他设置一起保存
- 浏览器重启后保持状态

## 消息通信

### Popup → Background

```javascript
// 切换剪贴板净化
chrome.runtime.sendMessage({ action: 'toggleClipboardCleaning' })

// 切换短链接解析
chrome.runtime.sendMessage({ action: 'toggleShortUrlExpansion' })

// 获取功能状态
chrome.runtime.sendMessage({ action: 'getFeatureStatus' })
```

### Background → Popup

```javascript
// 返回切换结果
sendResponse({ enabled: true/false })

// 返回功能状态
sendResponse({
  clipboardCleaningEnabled: true/false,
  shortUrlExpansionEnabled: true/false
})
```

## 文件修改清单

1. **manifest.json**
   - 添加 `clipboardRead`, `clipboardWrite` 权限
   - 添加 `alarms`, `notifications` 权限

2. **shortener_domains.json** (新增)
   - 短链接服务商域名列表
   - 60+ 常见短链接服务

3. **popup.html**
   - 添加"高级功能"部分
   - 添加两个功能开关

4. **popup.css**
   - 添加功能开关样式
   - 添加悬停效果

5. **popup.js**
   - 添加功能状态加载
   - 添加开关事件监听
   - 添加切换方法

6. **background.js**
   - 添加剪贴板监控逻辑
   - 添加短链接解析逻辑
   - 添加消息处理

## 故障排除

### 剪贴板净化不工作

**可能原因**：
- 功能未启用
- 剪贴板内容不是有效URL
- 浏览器焦点问题

**解决方案**：
1. 检查功能开关是否打开
2. 确认复制的是完整URL
3. 等待2秒后再粘贴

### 短链接解析失败

**可能原因**：
- 功能未启用
- 网络连接问题
- 短链接服务不可用
- 域名不在列表中

**解决方案**：
1. 检查功能开关是否打开
2. 检查网络连接
3. 手动访问短链接
4. 报告未支持的短链接服务

### 通知不显示

**可能原因**：
- 浏览器通知权限未授予
- 系统通知被禁用

**解决方案**：
1. 检查浏览器通知权限
2. 检查系统通知设置
3. 功能仍然正常工作，只是没有通知

## 性能影响

### 剪贴板监控

- **CPU使用**：极低（每2秒一次检查）
- **内存使用**：< 1MB
- **电池影响**：可忽略

### 短链接解析

- **网络请求**：HEAD请求，数据量极小
- **延迟**：通常 < 500ms
- **用户体验**：几乎无感知

## 未来改进方向

### 剪贴板净化

1. **智能检测**：只在复制URL时触发
2. **批量处理**：支持多个URL同时净化
3. **历史记录**：记录净化历史
4. **自定义通知**：允许用户自定义通知样式

### 短链接解析

1. **缓存机制**：缓存已解析的短链接
2. **预加载**：鼠标悬停时预解析
3. **域名更新**：自动更新短链接域名列表
4. **自定义域名**：允许用户添加自定义短链接域名

---

**这两个高级功能将 ClearURL 的保护能力提升到新的维度！** 🚀
